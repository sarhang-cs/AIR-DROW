{
  "name": "air-drow",
  "private": true,
  "version": "1.0.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check src/main.js && node --check src/data-vault.js && node --check src/api-client.js && node --check src/interaction-guard.js && node --check src/touch-engine.js && node --check src/hand-engine.js",
    "preflight": "node ../scripts/preflight.mjs"
  },
  "devDependencies": {
    "vite": "latest"
  },
  "dependencies": {
    "lottie-web": "5.12.2"
  },
  "description": "AirDraw production frontend by SARHANG IO"
}
