# AIR-DROW Hand Drawing Files

This folder contains the hand model used by AIR-DROW when you choose Camera and Hand drawing. It is included locally so the app can use hand tracking without downloading a model while you draw.

Do not rename or delete `hand_landmarker.task`. If hand drawing ever needs help, open Settings → About App and use App Check or Camera & Hand Check.
