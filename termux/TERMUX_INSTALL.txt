AIR-DROW v8.7.0 — Final Release + Font Restoration

1. Extract the release ZIP on Android.
2. From AIR-DROW-main, run:
   bash termux/replace-with-v870-final-release-font-restoration.sh "$HOME/AIR-DROW-GITHUB"

The workflow preserves .git, replaces application source, verifies the local Kurdish typography asset and production build, then commits/pushes only on success.
