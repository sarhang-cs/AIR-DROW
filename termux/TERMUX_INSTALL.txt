AIR-DROW v7.7.0 — Export, Gesture & Reliability

1. Put AIR-DROW_v7.7.0_EXPORT_GESTURE_RELIABILITY.zip in /sdcard/Download.
2. Extract it on Termux using the full command from the delivery message.
3. The transactional installer preserves .git, creates a rollback backup, validates npm run vercel:build, commits, and pushes only after all checks pass.
4. Vercel deploys automatically from the pushed branch.

The source tree is replaced as one clean release so old code, stale styles and previous cache identifiers cannot remain.
