AIR-DROW v7.6.2 — Layout & Input Integrity

1. Put AIR-DROW_v7.6.2_LAYOUT_INPUT_INTEGRITY.zip in /sdcard/Download
2. Copy the full Termux command from the delivery message.
3. It removes the old AIR-DROW repository checkout, extracts this complete archive, validates it, commits it and pushes main.
4. Vercel builds automatically from the pushed main branch.

The command intentionally replaces the old source tree so stale styles and old release identifiers cannot remain.
