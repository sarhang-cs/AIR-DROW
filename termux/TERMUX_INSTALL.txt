AIR-DROW v8.3.0 — Export Preview & Save Polish

1. Put AIR-DROW_v8.3.0_EXPORT_PREVIEW_SAVE_POLISH.zip in /sdcard/Download.
2. Extract it, then run termux/replace-with-v830-export-preview-save-polish.sh from the extracted AIR-DROW-main folder.
3. The script preserves .git, creates a rollback backup, runs npm ci + npm run verify:all + npm test, commits and pushes only after success.
4. After Vercel deploys, use Export Preview once, close it, then explicitly test PNG/JPG export on Android.
