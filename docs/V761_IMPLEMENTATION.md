# v7.6.1 implementation

This release uses native HTML/CSS/SVG animation for the hand tutorial. It has no external CDN, Lottie runtime, image search asset, WebGL scene, or fabricated tracking data. The visual guide is offline-safe; MediaPipe remains the only real hand-landmark source.
